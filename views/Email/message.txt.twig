{{ message.author.name }} just sent a message on "{{ message.group.subject }}"
You can view the conversation by visiting the following link:

{{ message.group.url('show', true) }}

The message content is as follows:

{{ message.content }}
