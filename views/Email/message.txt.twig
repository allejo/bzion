{{ message.author.name }} just sent a message on "{{ message.conversation.subject }}"
You can view the conversation by visiting the following link:

{{ message.conversation.url('show', true) }}

The message content is as follows:

{{ message.content }}
